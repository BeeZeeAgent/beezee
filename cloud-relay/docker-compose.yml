services:
  launchpad-cloud-relay:
    build: .
    container_name: launchpad_cloud_relay
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-8789}:8789"
    volumes:
      - ./data:/app/data
